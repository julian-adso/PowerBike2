// MotoAccesorios Interactive Script
// For now, this script is a placeholder.
// Future interactivity can be added here.

document.addEventListener('DOMContentLoaded', () => {
    console.log('MotoAccesorios homepage loaded and script running.');

    // Example: Smooth scroll for anchor links (if any added later)
    // document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    //     anchor.addEventListener('click', function (e) {
    //         e.preventDefault();
    //         document.querySelector(this.getAttribute('href')).scrollIntoView({
    //             behavior: 'smooth'
    //         });
    //     });
    // });
});

